import React,{useState} from 'react'; import {createRoot} from 'react-dom/client'; import {Bot,Plus,Send,Settings2} from 'lucide-react'; import './style.css';
function App(){const [input,setInput]=useState(''); const [messages,setMessages]=useState<{role:string;content:string}[]>([]); const send=()=>{if(!input.trim())return; const q=input; setInput(''); setMessages(m=>[...m,{role:'user',content:q},{role:'assistant',content:'这是初始化阶段的演示回复。下一步将接入 Agent 和工具调用。'}]);}; return <div className="app"><aside><div className="brand"><Bot size={22}/> AI Workbench</div><button className="new" onClick={()=>setMessages([])}><Plus size={17}/> 新建会话</button><div className="section">会话</div><div className="session active">初始化工作台</div><div className="bottom"><Settings2 size={17}/> 设置</div></aside><main><header><div><h1>初始化工作台</h1><span>Agent 工具调用实验室</span></div><select><option>Mock Model · local</option></select></header><div className="chat">{messages.length===0?<div className="empty"><Bot size={42}/><h2>开始一次对话</h2><p>输入任务，后续 Agent 将在这里展示工具选择与执行结果。</p></div>:messages.map((m,i)=><div className={'msg '+m.role} key={i}><b>{m.role==='user'?'你':'Agent'}</b><div>{m.content}</div></div>)}</div><div className="composer"><textarea value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder="描述一个任务，例如：查询现在的时间"/><button onClick={send} title="发送"><Send size={18}/></button></div></main></div>}; createRoot(document.getElementById('root')!).render(<App/>);
